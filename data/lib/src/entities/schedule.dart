import 'package:data/data.dart';

class Schedule {
  String objectId;
  DateTime startDate;
  DateTime endDate;

  Schedule({
    required this.objectId,
    required this.startDate,
    required this.endDate,
  });
}
